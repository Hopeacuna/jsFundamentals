let number = 10;
console.log(number);

//string strings are datatypes used to represent text wrap them in single or double quotes

let stringOne = 'double quoites';
let stringTwo = "single quotes";

console.log(stringOne, stringTwo);

// booleans true or false 

let on = true;

let off = false;

console.log(on, off);

//null null is an empty value 

let empty = null;

console.log(empty);

//undefined = no value is assigned and does not act as an empty container

let undef = undefined;
console.log(undef);

// null is like a container with nothing in it, undefined, the variable has never been set or has not yet been created

// objects = usewd to store many values instead iof one single one\
// denoted bu curley braces {}

let frodo = {
    race: 'hobbit',
    rings: 1,
    cloak: true
}

console.log(frodo);
console.log(typeof frodo);

// arrays containers that hold a list of items
// denoted by square brackets []

let list = ['item1', 'item2', 'item3'];

console.log(list);

let burritos = ['large', 4, true];
console.log(burritos);

console.log(typeof burritos);

// datatype literals
// a literal represents a fixed value that we as devs insert code 
// includes strings numbers booleans arrays 

// string literal
let car = 'Ford';

//number literal
let december = 12;

//boolean literal

let tired= true;

// object literal 
let soup = {
    a: 'chicken noodle',
    b: 'tomato soup',
    c: 'beef and barley'
};

// array literal

let days = ['monday', 'tuesday', 'wednesday', 'thurday', 'thursday', 'friday'];




